# 変数`dmg_s`に文字列「999」を代入
dmg_s = "999"
# `dmg_s`を文字列のまま+でつなげて出力
print("最大ダメージ値" + dmg_s + "を文字列として保持")
# `dmg_s`の値を整数に変換して`dmg_i`に代入
dmg_i = int(dmg_s)
# `dmg_i`の値を整数として出力
print("int()でdmg_sを整数", dmg_i, "に変換")